apiVersion: v1

kind: Pod

metadata:
  name: nodehelloworld.com
  labels:
    app: helloworld

spec:
  containers:
  - name: docker-demo
    image: dreiv/docker-demo
    ports:
    - containerPort: 3000